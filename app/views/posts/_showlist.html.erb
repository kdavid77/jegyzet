<ul>
  <% allposts=Post.all %>
  <% allposts.each do |f| %>
     <li>
       <%= link_to f.title, f %>
       <%= link_to "del", f, method: :delete, data: { confirm: "Biztos?" }, class: "post-del" %>
     </li>
  <% end %>
</ul>